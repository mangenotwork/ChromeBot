v0.1.1 前必修
- [bug] chrome click=NowTabMatchDemoContentOP("百度一下") // 获取当前页面内容为“百度一下”可交互的xpath 功能失效
- [bug] chrome click=`//*[@id="chat-submit-button"]` // 点击确定 功能失效

v0.1.1 之后修
- [bug] 乱输入的函数应该被提示，未知函数，而不是什么都没有
- 支持 | 管道符，上个命令的输出作为下个命令的开始
- 当前电脑卡的时候chrome进程实际上未启动，但是pid已经检查到了，导致后续无法连接上chrome的调试
- 语法 : 支持对象，希望如果是mysql这种的话能直接对象变量点方法(如: mysql.conn().... )
- 将html table 直接转存到excel文件 没有提取表头 th
- excel 检查当前文件是否被打开（是否被占用） 
- 字典还是需要保证顺序体验更好一些
- [bug] chrome提取页面html的iframe提取不到 
```  
[] 3. 采集中国体彩的大乐透历史开奖数据到excel文件  https://www.lottery.gov.cn/kj/kjlb.html?dlt
```
- gaokaochsi_2.cbs 需要简化，脚本语言看着函数调用太多，增加了脚本代码量违反了设计初衷
- douban_1.cbs 需要简化，脚本语言看着函数调用太多，增加了脚本代码量违反了设计初衷
