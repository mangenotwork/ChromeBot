- 需要支持 i++ i-- 
```cbs
var i=0
print(i++)
```

- 简化 for, while 遍历list,dict 
```
var data = {"one": 1, "two": 2}
var pairs = items(data)
var k = 0
while k < len(pairs) {
    var pair = pairs[k]
    print("键:", pair[0], "值:", pair[1])
    k = k + 1
}

改为 

while key in data {
     print("键:", key, "值:", data[key])
}

for key in data {
     print("键:", key, "值:", data[key])
}

如果是列表
while i in data {
     print( "值:", data[i])
}

for i in data {
     print("键:", key, "值:", data[i])
}


```

- builtinHas 方法需要 List类型也要支持

- 内置函数的错误没有抛出给终端

- 