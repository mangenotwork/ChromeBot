-  字典类型的打印将map[...]改为dict[...]
```cbs
var d1 = {"one": 1, "two": 2}
print(d1)
```

- else if 语法不支持,需要支持这个语法
```cbs
var a = 10

if a > 5 {
    print("a > 5")
} else if a > 8 {
    print("a > 8")
} else if a > 10 {
    print("a > 10")
}
```

- 需要支持 switch case 语法

- 

